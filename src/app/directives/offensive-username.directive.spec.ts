import { OffensiveUsernameDirective } from './offensive-username.directive';

describe('OffensiveUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new OffensiveUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
