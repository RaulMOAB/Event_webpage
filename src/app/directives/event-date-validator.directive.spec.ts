import { EventDateValidatorDirective } from './event-date-validator.directive';

describe('EventDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EventDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
